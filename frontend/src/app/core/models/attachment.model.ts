export interface AttachmentDto {
  id: string;
  fileName: string;
  fileUrl: string;
  contentType: string;
  fileSize: number;
  entityType: string;
  entityId: string;
  uploadedById: string;
  createdAt: string;
}
